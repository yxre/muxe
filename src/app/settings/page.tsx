import {Container} from "@/components/Container";


export default function Settings() {
    return <Container>
        Settings
        <div>
            minimum required tags
        </div>
        <div>
            directory to use
        </div>
    </Container>
}